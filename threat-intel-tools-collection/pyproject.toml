[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "threat-intel-tools-collection"
version = "0.1.0"
description = "Coleção Threat Intelligence do repositório Security-Guide"
readme = "README.md"
requires-python = ">=3.10"
authors = [ { name = "gtopfer" } ]

[project.scripts]
ti-collector = "threat_intel_tools_collection.cli:main"

[tool.setuptools]
packages = { find = {} }
